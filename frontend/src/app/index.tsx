import { View } from "react-native";

// This file is required by Expo Router to generate the root index.html file
// when exporting the web app. The actual routing logic and redirects to 
// /login or /private happen in _layout.tsx
export default function Index() {
	return <View />;
}
